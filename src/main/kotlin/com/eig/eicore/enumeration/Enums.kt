package com.eig.eicore.enumeration

enum class Gender {  MALE,FEMALE }

enum class MaritalStatus { MARRIED, SINGLE, DIVORCED, WIDOWER }

enum class EntityEnum { APPLICATION, NONE, PERMISSION, ROLE, IDENTITY_CARD, PERSON }

enum class RoleType { APPLICATION, SYSTEM }

enum class ActionBody { SAVE, UPDATE }